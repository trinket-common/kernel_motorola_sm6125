config INPUT_ABOV
        tristate "ABOV_SAR_sensor"
        depends on I2C
        default n
        help
          Say Y to enable the SAR detection application, if N here not to use the
          this SAR sensor
